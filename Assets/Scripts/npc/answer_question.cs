using UnityEngine;
using System.Collections;
using System.Collections.Generic;
using Flower;
using UnityEngine.SceneManagement;
using System;

public class answer_question : MonoBehaviour
{
    FlowerSystem fs;
    void Start()
    {

    }

    void Update()
    {
        /*
        fs = FlowerManager.Instance.GetFlowerSystem("default");
        
        fs.SetupDialog();
        fs.ReadTextFromResource("answer_question");
        fs.RegisterCommand("lock",(List<string> _params)=>{   
              
        });

        fs.RegisterCommand("release",(List<string> _params)=>{       
            //balloon.GetComponent<balloon>().canPlay = false ;  
            // 找到場景中的 balloon 對象
            GameObject balloonObject = GameObject.Find("balloon"); // 確保名字與場景中的物件名稱一致
            ballloon script = balloonObject.GetComponent<ballloon>();
            script.canPlay = true;

            GameObject balloonObject1 = GameObject.Find("balloon1"); // 確保名字與場景中的物件名稱一致
            pink script1 = balloonObject1.GetComponent<pink>();
            script1.canPlay = true;

            GameObject balloonObject2 = GameObject.Find("balloon2"); // 確保名字與場景中的物件名稱一致
            black script2 = balloonObject2.GetComponent<black>();
            script2.canPlay = true;       
                  
        });
        */

    }
    
}
